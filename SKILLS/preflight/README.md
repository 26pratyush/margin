# Preflight skill placeholder

Future scope: run formatting, linting, tests, builds, documentation checks, synthetic-data checks, and the final privacy review before a Pull Request.

When implemented, add `SKILL.md` with stack-specific commands and failure-handling guidance.

# Margin app

This directory will contain the local-first finance application.

The implementation stack is intentionally undecided until the first foundation epic. The app should remain a simple local application with a clear persistence boundary, a testable calculation layer, and no dependency on the hosted product website.
